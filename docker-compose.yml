services:
  kindle-email:
    build: .
    restart: unless-stopped
    volumes:
      - ./config.toml:/app/config.toml:ro
    environment:
      - KINDLE_EMAIL_IMAP_PASSWORD
      - KINDLE_EMAIL_SMTP_PASSWORD
      - KINDLE_EMAIL_RAINDROP_TOKEN
      - KINDLE_EMAIL_DISCORD_WEBHOOK
